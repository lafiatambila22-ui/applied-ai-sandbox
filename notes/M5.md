# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — <name>**: <what changes>. _Verify: <how>._
2. **Task 2 — <name>**: ...
3. ...

## Task 1 context brief
<paste the full brief you'll give Claude>

## Task 1 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 2 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 3 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Reflection — where I redirected
-

## Decomposition (8-10 tasks)
1. **Task 1 — Extend note dict**: Add a tags key (list of strings, default empty) to app.notes.append(). Verify: existing tests still pass and a new note has tags == [].
2. **Task 2 — Update new-note form**: Add a comma-separated tags input field to new_note.html. Verify: field appears in the browser.
3. **Task 3 — Parse tags in POST handler**: Split the tags input by comma, strip whitespace, and save to the note dict. Verify: creating a note with "work, urgent" yields note["tags"] == ["work", "urgent"].
4. **Task 4 — Display tags as chips**: Show each tag as a styled chip next to the note title in home.html. Verify: tags appear visually in the browser.
5. **Task 5 — Build filter bar**: Add an All and per-tag filter bar at the top of home.html using unique tags from all notes. Verify: filter bar appears in the browser.
6. **Task 6 — Wire up filter logic**: Accept a tag query param in the home route and filter notes by it. Verify: clicking a tag shows only matching notes.
7. **Task 7 — Write tag parsing tests**: Test that comma-separated input produces the correct tags list. Verify: pytest passes.
8. **Task 8 — Write filter behavior tests**: Test that filtering by tag returns only notes with that tag. Verify: pytest passes.

## Task 1 context brief
Goal: Add a tags key to the note dict in app.py.

Constraints:
- tags is a list of strings, default empty list.
- Only change app.py where notes are appended.
- Do not touch any templates or tests yet.

Files to change:
- app.py (notes.append line only)

Acceptance:
- A new note created via POST has tags == [].
- All existing tests still pass.

Plan first, don't write code yet.

## Task 1 — what happened
- Claude's plan summary: Added tags key with default empty list to the notes.append call in app.py. Also completed Task 02 delete route.
- What I pushed back on: Nothing, the plan was scoped correctly.
- Verification result: All 10 tests passed.
